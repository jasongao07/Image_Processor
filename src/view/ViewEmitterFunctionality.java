package view;

/**
 * The interface for a view emitter functionality.
 */
public interface ViewEmitterFunctionality {

  /**
   * Adds a listener.
   *
   * @param viewListener the listener to be added
   */
  void addListener(ViewListener viewListener);
}
