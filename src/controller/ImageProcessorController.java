package controller;

/**
 * This interface acts as a controller for an image processor. It can be used to direct
 * information amongst the model and view. It runs the controller from start to finish.
 */
public interface ImageProcessorController {

  /**
   * Runs the controller.
   */
  void run();
}
